services:
  - type: web
    name: cosmetic-analyzer-api
    runtime: node
    rootDir: cosmetic-analyzer/backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 3001
    healthCheckPath: /health
